<% content_for :content do %>
  <table class="table">
    <thead>
      <tr>
        <th>項目</th>
        <th>内容</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th>ID</th>
        <td><%= @admin.id %></td>
      </tr>
      <tr>
        <th>名前</th>
        <td><%= @admin.name %></td>
      </tr>
      <tr>
        <th>メールアドレス</th>
        <td><%= @admin.email %></td>
      </tr>
    </tbody>
  </table>
  <%= button_to '削除', [:admin, @admin], method: :delete, data: { confirm: '本当に削除しますか？' }, class:"btn btn-danger" %>

<% end %>
<%= render :template => 'admin/frame' %>
<%
=begin
%>
<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @admin.email %>
</p>

<p>
  <strong>Password:</strong>
  <%= @admin.password %>
</p>

<p>
  <strong>Password confirmation:</strong>
  <%= @admin.password_confirmation %>
</p>

<%= link_to 'Edit', edit_admin_admin_path(@admin) %> |
<%= link_to 'Back', admin_admins_path %>

<%
=end
%>
